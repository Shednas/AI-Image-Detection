# Test dependencies. Install on top of requirements.txt to run the suite.
#   pip install -r requirements.txt -r requirements-dev.txt

pytest>=9.0
pytest-cov>=5.0

# required by starlette's TestClient
httpx>=0.28
